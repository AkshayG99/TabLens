# TabLens GRPO training requirements (server/Linux friendly).
# Install: pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu126

# --- core training stack ---
torch==2.13.0+cu126
transformers==5.6.0
trl==0.24.0            # GRPOTrainer
peft==0.18.1
datasets==4.0.0
accelerate==1.11.0
bitsandbytes>=0.44     # needed by the 4070 profile (--quantize 4bit); unused on bf16 servers but harmless
safetensors==0.8.0
huggingface_hub==1.24.0

# --- pinned transitives that matter ---
numpy==2.5.1
tqdm==4.69.0
PyYAML==6.0.3
packaging==26.2

# --- fast kernels (liger-loss / --disco, Qwen3.5 linear-attention layers) ---
# Unpinned, matching scripts/run_sft_a6000.sh's own convention for these same
# three packages -- fast-moving, no confidently-verifiable exact pin without
# testing the build on real hardware (causal-conv1d / flash-linear-attention
# compile native CUDA extensions; the system CUDA toolkit must match the
# torch build above or the build fails, see run_disco.sh's comments).
liger-kernel            # LigerFusedLinearGRPOLoss (rl_grpo/train_grpo.py's use_liger_loss)
flash-linear-attention  # same; best-effort, not required for correctness

# --- optional ---
# vllm                  # only if training with --use-vllm
# hf_transfer==0.1.9    # faster HF downloads (set HF_HUB_ENABLE_HF_TRANSFER=1)

# --- NOT needed for GRPO (uncomment if regenerating dataset or running eval) ---
# pandas==2.3.3         # prepare_data.py, data/*
# scikit-learn==1.9.0   # data/preprocess.py, eval/metrics.py
# ucimlrepo==0.0.7      # test.py raw data fetch
